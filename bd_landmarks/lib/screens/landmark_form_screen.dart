import 'package:flutter/material.dart';

class LandmarkFormScreen extends StatelessWidget {
  const LandmarkFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Landmark'),
      ),
      body: const Center(
        child: Text('Landmark Form - Coming Soon'),
      ),
    );
  }
}

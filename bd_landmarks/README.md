# bd_landmarks

Simple Flutter app scaffold for BD Landmarks. The home screen is a placeholder that shows "BD Landmarks App" inside a MaterialApp shell.

## Run locally

- `flutter pub get`
- `flutter run`

## Notes

- Uses the default Flutter dependencies only; no extra packages are added.
